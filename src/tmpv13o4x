#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}

use wasm_bindgen::prelude::*;

// #[wasm_bindgen]
// extern {
    
// }

#[wasm_bindgen]
pub fn create_grid(width: u32, height: u32) -> Result<JsValue, JsValue> {
    set_panic_hook();

    let js_result: JsValue = JsValue::from_serde(&f).unwrap()
    Ok(js_result) 
}